version: 2
jobs:
  build:
    docker:
            - image: holochain/holochain-rust:latest

    steps:
      - checkout

      - run:
          name: build dna
          command: cd dna-src  && hc package

      - run:
          name: Print the Current Time
          command: date
