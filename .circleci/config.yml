version: 2
jobs:
  build:
    docker:
            - image: holochain/holochain-rust:latest

    steps:
      - checkout

      - run:
          name: build hc cli
          command: hc-cli-install

      - run:
          name: build dna
          command: cd dna-src && hc package
